{% extends '::base.html.twig' %}
{%block corps%}
    <div class="row">
        <div class="large-12">
            <h1 class="bleu">/// Les groupes</h1>
        </div>
    </div>

    {% for entity in entities %}

    
          
        <div class="row">
            <div class="large-12">
                <div class="row blocBlanc">
                    <h5><a class="bleu" href="{{ path('groupe_show', { 'id': entity.id }) }}">{{ entity.titre }}</a></h5>
                    <div class="large-2 columns columns">
                        {% media entity.image, 'small' %}
                    </div>
                    <div class="large-10 columns">
                        
                        
                    <div class="row"></div>
                        <p>{{ entity.teaser | raw }}</p>
                    </div>
                </div>
            </div>
        </div>
            
    {% endfor %}

{%endblock%}