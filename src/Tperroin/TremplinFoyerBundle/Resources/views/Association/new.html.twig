<h1>Association creation</h1>

<form action="{{ path('association_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('association') }}">
            Back to the list
        </a>
    </li>
</ul>
